import 'package:flutter/material.dart';

class LastClassesVisitedList extends StatelessWidget {
  const LastClassesVisitedList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
